#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='Hopfield Boosting',
      version='1.0',
      packages=find_packages(include=['hopfield_boosting', 'hopfield_boosting.*'])

     )